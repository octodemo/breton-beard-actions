name: hello-goodbye
on:
    workflow_dispatch:
jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
        - run: echo "Hello world"
    goodbye:
        runs-on: ubuntu-latest
        steps:
        - run: echo "Goodbye world"
        needs: hello